# تقرير جاهزية B-Attend للبيع والتشغيل

**الحالة الحالية:** Release Candidate قابل للنشر بعد ضبط بيئة الإنتاج وقاعدة البيانات والبريد والدفع والمراقبة. تم تنفيذ إصلاحات فعلية داخل المستودع، وليس مجرد توصيات.

## النتيجة التنفيذية

تم تحويل نقاط الانهيار التي ظهرت أثناء التشغيل المحلي إلى مسارات مستقرة في الواجهة العامة. كانت الصفحة الرئيسية تنهار بخطأ HTTP 500 عند غياب PostgreSQL، كما كانت صفحات الأسعار والتواصل وطلب العرض والتسجيل تحتوي على تعارض بين مكونات الخادم والعميل. أصبح الموقع العام يعرض الصفحة والأسعار ومسارات التحويل الأساسية حتى عند تعذر قاعدة البيانات، مع إبقاء مصدر البيانات الحقيقي هو PostgreSQL عند توفره.

تمت إضافة تموضع منتجي واضح يميز B-Attend عن أنظمة HRMS العامة: **منصة تشغيل للفروع تسد الفجوة بين تسجيل الحضور ويوم الرواتب**. الرسالة الجديدة تركز على رؤية خطر نقص الطاقم، حل الاستثناءات بسرعة، وتصدير نتيجة قابلة للتدقيق. هذا التوجه يتوافق مع ما يعلنه المنافسون، لكنه يضع B-Attend في زاوية تشغيلية أكثر تحديدًا بدل منافسة المنصات الشاملة على عدد الميزات فقط.

## دراسة المنافسة والتميّز المقترح

| المنافس | ما يعلنه علنًا | نقطة القوة | قرار التميّز لـ B-Attend |
|---|---|---|---|
| Truein | حضور سحابي، تعرّف على الوجه، تطبيق جوال، تحديد جغرافي، سياسات قابلة للتخصيص، تقارير جاهزة للرواتب، جدولة وتغطية مواقع متعددة [1] | قوة واضحة في الحضور الميداني والموظفين الموزعين | التركيز على **إدارة الاستثناء والقرار التشغيلي** بعد تسجيل الحضور، مع دعم عربي ومسارات موافقة وسجل تدقيق |
| Bayzat | رواتب سحابية، WPS وGOSI وEOSB، سير عمل موافقات، RBAC، سجلات تدقيق، تكاملات محاسبية، عملات متعددة [2] | منصة HR/payroll واسعة وذات تموضع مؤسسي | عدم تقليد اتساع HRMS؛ تقديم **تشغيل فرعي أسرع وأخف** للفرق الأمامية متعددة المواقع |
| ZenHR | منصة ثنائية اللغة للسوق السعودي، توافق مع نظام العمل وGOSI وMudad وMuqeem، رواتب، EOSB، خدمة ذاتية، حضور وإجازات [3] | التوطين والامتثال السعودي | عدم ادعاء تكاملات بلد محدد قبل تنفيذها؛ بناء طبقة تشغيل عربية قابلة للتوسع وإضافة موصلات الامتثال حسب البلد |
| Zimyo | HRMS مدعوم بالذكاء الاصطناعي، WPS، مكافأة نهاية الخدمة، حضور GPS، مزامنة أجهزة حيوية، العطلات، العمل الإضافي، مستندات عربية وإنجليزية [4] | اتساع المنصة والدليل الاجتماعي | استخدام B-Coach كميزة مساعدة، مع جعل الميزة الأساسية **إغلاق دورة الحضور إلى الرواتب بتدقيق واضح** |

التميّز الذي تم إدخاله في الصفحة الرئيسية ليس ادعاءً جديدًا منفصلًا عن الكود؛ فهو مبني على وحدات موجودة في المستودع مثل الحضور الجغرافي، الورديات، الموافقات، التقارير الجاهزة للرواتب، السجل التدقيقي، التعدد المؤسسي، وB-Coach كما يوضح المستودع نفسه [5]. يجب تسويق التكاملات التنظيمية مثل GOSI أو WPS أو Mudad أو Muqeem فقط بعد تنفيذ كل موصل واختباره في البلد المستهدف.

## الأعمال المنفذة داخل المستودع

| المجال | ما تم تنفيذه |
|---|---|
| استقرار الموقع العام | إضافة `src/lib/public-plans.ts` لتحميل الخطط من PostgreSQL عند توفرها، واستخدام خطط احتياطية متطابقة مع بيانات seed عند تعذر الاتصال أو عدم وجود خطط نشطة |
| صفحة الأسعار | إزالة تعارض Server/Client Component، إبقاء `PublicLayout` في الصفحة الخادمية، وتمرير `PricingClient` كمكون تفاعلي فقط؛ إصلاح تنسيق `EGP//mo` إلى `EGP /mo` |
| التسجيل | ربط صفحة `/signup` بمصدر الخطط المتسامح مع فشل قاعدة البيانات، وتغليف النموذج التفاعلي داخل غلاف خادمي صحيح |
| التواصل وطلب العرض | إزالة استيراد `PublicLayout` من الصفحات العميلية لمنع أخطاء `getTranslations` داخل Client Components |
| التسويق والتميّز | إضافة قسم تشغيلي جديد يوضح رؤية خطر نقص الطاقم، حل الاستثناءات، والتصدير القابل للتدقيق |
| العربية وRTL | إضافة مفاتيح الترجمة الجديدة باللغتين، تعريب اسم الباقة في التسجيل والصفحة الرئيسية، وإصلاح كلمة الربط الإنجليزية داخل عبارة الموافقة على الشروط |
| الاعتمادات | تحديث `package-lock.json` بعد اكتشاف عدم توافقه مع `package.json`؛ أصبح التثبيت النظيف ممكنًا بعد الإصلاح |

## التحقق المنفذ

| الفحص | النتيجة |
|---|---|
| `npm run typecheck` | ناجح |
| `npm run lint` | ناجح |
| `npm run i18n:check` | ناجح؛ 2770 مفتاحًا في كل لغة، ولا توجد مفاتيح مفقودة |
| `npm run build` | ناجح؛ تم توليد جميع المسارات وتطبيق postbuild |
| `/` دون PostgreSQL | ناجح؛ الصفحة تعرض fallback عام بدل 500 |
| `/pricing` دون PostgreSQL | ناجح؛ الخطط الخمس وجدول المقارنة يظهران |
| `/contact` | ناجح؛ النموذج يظهر دون خطأ Server/Client |
| `/request-demo` | ناجح؛ النموذج والحقول المطلوبة يظهران |
| `/signup` | ناجح؛ الخطط، دورة الفوترة، الشروط والخصوصية تظهر |
| العربية وRTL | ناجح؛ الصفحة الرئيسية والتسجيل يعرضان تخطيط RTL ونصوصًا عربية |

## متطلبات الإطلاق التجاري المتبقية

هذه المتطلبات خارج ما يمكن إثباته بأمان داخل بيئة محلية بلا PostgreSQL أو بيانات إنتاج. يجب ضبط `DATABASE_URL` و`DIRECT_URL` على PostgreSQL فعلي، تنفيذ migrations، تشغيل seed تجريبي فقط في بيئة demo، ثم اختبار إنشاء الشركة وتسجيل الدخول والحضور والموافقات والتقارير ببيانات غير تجريبية. يجب تغيير كلمات مرور الحسابات التجريبية وإجبار أي حساب تم إنشاؤه من seed على تغيير كلمة المرور قبل الإنتاج.

كما يجب تفعيل SMTP حقيقي مع SPF وDKIM وDMARC، اختيار بوابة دفع فعلية بدل `MANUAL_ACTIVATION_MODE`، ضبط مفاتيح مزود الذكاء الاصطناعي إن كان B-Coach سيباع كميزة حقيقية، إضافة مراقبة للأخطاء والتنبيهات والنسخ الاحتياطية مع اختبار استعادة، ومراجعة الشروط وسياسة الخصوصية قانونيًا قبل البيع. توضح وثائق المشروع نفسها أن هذه العناصر مطلوبة قبل الإنتاج النهائي، ولذلك لا ينبغي وصف النسخة بأنها SaaS إنتاجي مكتمل قبل تنفيذها [5].

## توصية البيع والتسعير

ينبغي إطلاق B-Attend أولًا على شريحة المطاعم والمقاهي وسلاسل التجزئة والعمليات متعددة الفروع في مصر، برسالة بيع واحدة: **اعرف من لم يحضر، لماذا، وما الإجراء المطلوب قبل إغلاق الرواتب**. التجربة المجانية الحالية مناسبة لخفض الاحتكاك، بينما تكون باقة Growth هي الباقة المحورية للفرق متعددة الفروع. قبل تفعيل الدفع الآلي، يظل التفعيل اليدوي مناسبًا لمرحلة pilot بشرط وجود SLA واضح ومراجعة يدوية للعميل.

## المراجع

[1]: https://truein.com/gcc-blogs/best-employee-management-systems-middle-east "Truein — 5 Best Employee Management Systems in the Middle East"

[2]: https://www.bayzat.com/payroll "Bayzat — Payroll Management System"

[3]: https://www.zenhr.com/en/hr-software-saudi-arabia "ZenHR — Saudi-Compliant HR & Payroll Software"

[4]: https://www.zimyo.me/ "Zimyo — HR Software in the Middle East"

[5]: https://github.com/ganaatef/b-attend "B-Attend GitHub repository"
