# حالة الإطلاق الحالية

تم فحص مشروع `b-attend` في Vercel بتاريخ 21 أغسطس 2026. النشر الإنتاجي المرتبط بالنطاق `b-attend.vercel.app` في حالة **Ready** ويشغّل commit `87888c8` الذي يضيف إنشاء طلب الإجازة من تطبيق الموظفين. أظهرت لوحة المراقبة خلال آخر ست ساعات معدل أخطاء مقداره `0%`، مع 3.7K طلب Edge و844 استدعاء Function.

توجد في Vercel متغيرات `DATABASE_URL` و`DIRECT_URL` و`SESSION_SECRET` وبيانات المشرف ومتغيرات الدفع والتجربة. أضيفت بوابة مستقلة باسم `RUN_PRISMA_MIGRATIONS` لتُقيّد migrations ببيئة Production فقط عند النشر التالي، بدل تنفيذها على معاينات الفروع.

يبقى قبل البيع العام: تطبيق migration قاعدة البيانات للإصدار الخاص بمصدر حضور التطبيق، ضبط الأسرار الإنتاجية، اختبار Pilot على أجهزة فعلية، وتفعيل المراقبة/التحليلات والنطاق المخصص إن وُجد.
