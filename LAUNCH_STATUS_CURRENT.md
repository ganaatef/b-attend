# حالة الإطلاق الحالية

تم فحص مشروع `b-attend` في Vercel بتاريخ 21 أغسطس 2026. النشر الإنتاجي المرتبط بالنطاق `b-attend.vercel.app` في حالة **Ready** ويشغّل commit `f6a0049` الذي يضيف Vercel Analytics؛ وتوجد تحته التحديثات السابقة الخاصة بطلبات إجازة تطبيق الموظفين وبوابة migrations. أظهرت لوحة المراقبة خلال آخر ست ساعات معدل أخطاء مقداره `0%`، مع 3.7K طلب Edge و844 استدعاء Function.

توجد في Vercel متغيرات `DATABASE_URL` و`DIRECT_URL` و`SESSION_SECRET` وبيانات المشرف ومتغيرات الدفع والتجربة. أضيفت بوابة مستقلة باسم `RUN_PRISMA_MIGRATIONS` لتُقيّد migrations ببيئة Production فقط عند النشر التالي، بدل تنفيذها على معاينات الفروع.

تم حفظ `RUN_PRISMA_MIGRATIONS=true` داخل Vercel ضمن Production فقط، ثم دُفع commit `b5e5097` لتشغيل migration عند بنائه على بيئة الإنتاج.

اكتمل نشر Vercel للـ commit `b5e5097` بنجاح في بيئة Production خلال 33 ثانية، وأصبح في حالة **Ready**. هذا يؤكد أن بوابة migrations وعملية البناء اجتازتا دورة الإنتاج.

دُفع commit `7dff08b` لتصحيح رسالة الموقع العام بحيث يعلن تطبيق B-Attend Staff للموظفين، واكتمل نشره في Production بحالة **Ready** خلال 33 ثانية.

فُعّلت Vercel Web Analytics على خطة Hobby المجانية، ثم دُفع commit `f6a0049` الذي يضيف مكوّن Analytics إلى تخطيط Next.js الجذري. اكتمل نشر هذا commit في Production بحالة **Ready** خلال 59 ثانية. ستبدأ المنصة في جمع مؤشرات الزيارات والصفحات بعد زيارة الموقع من المستخدمين.

تتوفر Vercel Speed Insights مجانًا على خطة Hobby، لكنها ما زالت تتطلب ضغطة تفعيل من صفحة إعدادها المفتوحة في الحساب؛ لم تستجب نافذة التفعيل للنقر الآلي بعد محاولات متعددة.

يبقى قبل البيع العام: اختبار Pilot بحساب موظف حقيقي على Android وiPhone، اعتماد قرار تدوير `SESSION_SECRET` قوي إن لم يكن موثقًا، ضغطة تفعيل Vercel Speed Insights، وضبط نطاق مخصص وقناة دعم وفوترة فعلية قبل التوسع التجاري.
